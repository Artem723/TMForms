import React, { Component } from "react"

export default class SearchBar extends Component {
    render() {
        return (
            <div>
                Dashboard
            {" "}<input type="text"/>
            {" "}<button>Ok</button>
            {" "}<button>NEW</button>
            </div>
        )
    }
}